services:
  - type: web
    name: alex-ai-coach
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
